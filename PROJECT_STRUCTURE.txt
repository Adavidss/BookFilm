ShowBook Recommender - Complete Project Structure
==================================================

📦 showbook-recommender/
│
├─ 📄 Configuration Files
│  ├── package.json              # Dependencies & scripts
│  ├── tsconfig.json             # TypeScript config
│  ├── next.config.js            # Next.js config
│  ├── tailwind.config.js        # Tailwind CSS config
│  ├── postcss.config.js         # PostCSS config
│  ├── .env.example              # Environment variable template
│  └── .gitignore                # Git exclusions
│
├─ 📚 Documentation
│  ├── README.md                 # Main documentation (comprehensive)
│  ├── QUICKSTART.md             # 5-minute setup guide
│  ├── ARCHITECTURE.md           # Technical deep dive
│  ├── PROJECT_SUMMARY.md        # This overview
│  └── PROJECT_STRUCTURE.txt     # This file
│
├─ 📱 Application (app/)
│  ├── layout.tsx                # Root layout wrapper
│  ├── page.tsx                  # Books page (home) 📚
│  ├── globals.css               # Global styles
│  │
│  ├── shows/
│  │   └── page.tsx              # TV Shows page 📺
│  │
│  ├── discovery/
│  │   └── page.tsx              # Discovery page 🔍
│  │
│  └── cross-recommendations/
│      └── page.tsx              # Cross-recommendations page 🔀
│
├─ 🧩 Components (components/)
│  ├── Layout.tsx                # Main layout with navigation
│  ├── SearchBar.tsx             # Search input component
│  ├── BookCard.tsx              # Book display card
│  └── ShowCard.tsx              # TV show display card
│
├─ 🧠 Core Logic (lib/)
│  └── recommendationEngine.ts   # Recommendation algorithms
│                                  ├─ recommendBooks()
│                                  ├─ recommendShows()
│                                  ├─ recommendBooksFromShows()
│                                  └─ recommendShowsFromBooks()
│
├─ 🪝 React Hooks (hooks/)
│  └── useUserData.ts            # User data management hook
│                                  ├─ addBook() / removeBook()
│                                  ├─ addShow() / removeShow()
│                                  ├─ hasBook() / hasShow()
│                                  └─ localStorage integration
│
├─ 🔧 Utilities (utils/)
│  └── api.ts                    # API integration functions
│                                  ├─ TMDB API (TV shows)
│                                  │  ├─ searchTVShows()
│                                  │  ├─ getShowDetails()
│                                  │  ├─ getTrendingShows()
│                                  │  └─ getUpcomingShows()
│                                  │
│                                  └─ Google Books API (books)
│                                     ├─ searchBooks()
│                                     ├─ getBookDetails()
│                                     └─ getNewReleaseBooks()
│
├─ 📐 Types (types/)
│  └── index.ts                  # TypeScript type definitions
│                                  ├─ Book
│                                  ├─ TVShow
│                                  ├─ UserBook / UserShow
│                                  ├─ Recommendation
│                                  └─ Supporting types
│
└─ 💾 Data (data/)
   └── sample-collaborative.json # Example collaborative filtering data


Feature Breakdown by File
==========================

🎯 Pages (User-Facing Routes)
├─ app/page.tsx                  → Books: Search, track, recommend
├─ app/shows/page.tsx            → TV Shows: Search, track, recommend
├─ app/discovery/page.tsx        → Trending, new, upcoming content
└─ app/cross-recommendations/    → Books ↔ Shows cross-recommendations

🎨 UI Components
├─ components/Layout.tsx         → Header, nav, footer, page wrapper
├─ components/SearchBar.tsx      → Search input with loading states
├─ components/BookCard.tsx       → Book card with cover, genres, actions
└─ components/ShowCard.tsx       → Show card with poster, platforms, actions

⚙️ Core Engine
├─ lib/recommendationEngine.ts   → Content-based filtering algorithms
├─ hooks/useUserData.ts          → State management + localStorage
└─ utils/api.ts                  → External API integrations

📋 Type Safety
└─ types/index.ts                → All TypeScript interfaces


Data Flow
=========

Search Flow:
User Input → SearchBar → onSearch() → API Call → Parse → Display Cards

Add to List Flow:
Click "Add" → useUserData.addBook/Show() → State Update → localStorage → Re-render

Recommendation Flow:
User Data → recommendationEngine → Score Candidates → Sort → Display with Reasons


API Dependencies
================

Required:
✅ TMDB API (TV Shows)         → Free tier, 1000 req/day
                                 Get at: themoviedb.org

Optional:
⚪ Google Books API (Books)    → Works without key (rate limited)
                                 Better with key: console.cloud.google.com

Future:
🔮 Watchmode API (Streaming)   → For platform availability
🔮 Collaborative Data          → Manual curation or aggregation


Tech Stack
==========

Framework:    Next.js 14 (App Router)
Language:     TypeScript 5
UI Library:   React 18
Styling:      Tailwind CSS 3
Storage:      localStorage (client-side)
APIs:         TMDB, Google Books


Bundle Size (Estimated)
=======================

Main bundle:        ~150 KB (gzipped)
Next.js runtime:    ~80 KB
Tailwind CSS:       ~10 KB (purged)
Total:              ~240 KB

Acceptable for personal use ✅


Key Design Decisions
====================

✓ Client-side only       → No backend needed, deploy anywhere
✓ localStorage           → Simple persistence, privacy-focused
✓ Content-based          → Explainable, works with small datasets
✓ No AI/LLM APIs         → Deterministic, no cost/rate limits
✓ TypeScript             → Type safety, better DX
✓ Tailwind CSS           → Fast styling, easy customization
✓ Real APIs              → Legitimate data sources, no fakes


Extension Points
================

Easy Extensions:
├─ Add user ratings (1-5 stars)
├─ Export/import user data
├─ Filter by genre
├─ Sort options
└─ Keyboard shortcuts

Medium Extensions:
├─ Backend + user accounts
├─ Share lists with friends
├─ Multiple profiles
└─ Advanced filtering

Complex Extensions:
├─ Mobile app (React Native)
├─ Browser extension
├─ Real collaborative filtering
└─ Social features


Quick Commands
==============

Setup:
$ npm install
$ cp .env.example .env.local
$ [Add TMDB API key to .env.local]

Development:
$ npm run dev              # Start dev server (localhost:3000)
$ npm run build            # Build for production
$ npm start                # Run production build
$ npm run lint             # Run linter

Deployment:
$ vercel deploy            # Deploy to Vercel
$ npm run build && [host]  # Deploy to other platforms


What's Next?
============

1. Follow QUICKSTART.md for setup
2. Search for books and shows
3. Build your lists
4. See recommendations
5. Customize to your taste
6. Share with friends!


Philosophy
==========

"Simple, explainable, hackable. No AI hype, just useful logic."

Every line of code should earn its place.
Every recommendation should be explainable.
Every feature should be actually useful.

Keep it clean. Keep it simple. Make it yours. 🎬📚
